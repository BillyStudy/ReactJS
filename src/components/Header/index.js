import React from 'react'
import header from './header'

const Header = () => (
    <header style={header.container}>
        <h1 style={header.title}>Contatos</h1>
        <p style={header.slogan}>Curso react.js</p>    
    </header>
);


export default Header;